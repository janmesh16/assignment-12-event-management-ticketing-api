PORT=5000
JWT_SECRET=super_secret_event_ticketing_jwt_key_2026

# Firebase Credentials:
# For local dev, place your real serviceAccountKey.json in the project root.
# For production (e.g. Render), paste the entire JSON string of serviceAccountKey.json into FIREBASE_SERVICE_ACCOUNT.
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"..."}
